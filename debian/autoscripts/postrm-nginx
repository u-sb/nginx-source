if [ "$1" = "purge" ] ; then
	for conf in #NAMES# ; do
		if [ -e /etc/nginx/etc/nginx/modules-enabled/$conf ]; then
			rm /etc/nginx/etc/nginx/modules-enabled/$conf
		fi
	done

	if [ -s /run/nginx.pid ]; then
		nginx -s reload
        fi
fi
