#!/bin/sh
set -e

case "$1" in
  configure)
    TARGET="/usr/share/nginx/conf-available/brotli.conf"
    LINK="/etc/nginx/conf.d/brotli.conf"

    if [ ! -e "$LINK" ] && [ ! -L "$LINK" ]; then
      ln -s "$TARGET" "$LINK"
    fi
    ;;
esac

#DEBHELPER#

exit 0
